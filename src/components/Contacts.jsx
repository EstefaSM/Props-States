function Contacts(props){
    return(
        <div class="contacts">
            <p>Name: {props.name}</p>
            <p>Description: {props.description}</p>
        </div>
    );
}

export default Contacts;